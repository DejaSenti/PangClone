using UnityEngine;

public static class BallData
{
    public static int NUM_SPLIT_BALLS = 2;
    // The array MUST be the same size as the integer!
    public static Vector2[] DIRECTION_VECTORS = new Vector2[] { new Vector2(1, 1), new Vector2(-1, 1) };
}